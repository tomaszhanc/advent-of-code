import {GuardPosition} from "../model/GuardPosition";
import {Grid} from "../../../shared/grid/Grid";
import {Stack} from "../../../shared/Stack";
//
// function obstaclesToLoopTheGuard(guardPosition: GuardPosition, map : Grid<Tile>): number {
//     const possibleObstacles = new Set<string>();
//     const stack = new Stack<GuardPosition[]>();
//     let pathChecked = 0;
//
//     stack.push([guardPosition]);
//
//     while (!stack.isEmpty()) {
//         const currentGuardPath = stack.pop();
//         const nextPosition = nextGuardPosition(lastElement(currentGuardPath), map);
//
//
//     }
//
//     return 0;
// }
//
